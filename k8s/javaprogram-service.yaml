apiVersion: v1
kind: Service
metadata:
  name: java-program-service
  namespace: java-program
spec:
  selector:
    app: java-program
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: NodePort